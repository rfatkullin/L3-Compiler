func main (args->[[char]]) -> int

	for a = 0 -> int to 5 do
		print(a);
	endfor;

	for b = 0 -> int to 5 step 2 do
		print(b);
	endfor;


	for i = 'a' -> char to 'z' do
		print(i);
	endfor;

	for j = 'A' -> char to 'Z' do
		print(j);
	endfor;

	main = 0;
endfunc
